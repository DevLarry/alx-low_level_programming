#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - entry point
* Description: this is where all code is
* Return: 0
*/
int main(void)
{
	int alpha;

	for (alpha = 0; alpha <= 9; alpha++)
		printf("%d", alpha);
	printf("\n");
	return (0);
}
